CREATE TABLE carDatabase(id BIGINT PRIMARY KEY AUTO_INCREMENT
                                ,brand VARCHAR(20)
                                ,model VARCHAR(30)
                                ,colour VARCHAR (30)
                                ,dateOfProduction VARCHAR (10)
                                ,vin VARCHAR (20),
                                PRIMARY KEY (id));
